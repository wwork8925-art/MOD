// =============================================================================
// UserDtos.cs — حاويات بيانات المستخدمين
// =============================================================================
// تُستخدم هذه الحاويات في لوحة تحكم الأدمن لعرض وإدارة المستخدمين.
// =============================================================================

namespace backend.DTOs
{
    // -------------------------------------------------------------------------
    // بيانات المستخدم — ترسل للأدمن عند طلب قائمة المستخدمين
    // لا تحتوي على كلمة المرور أو بيانات Identity الداخلية
    // -------------------------------------------------------------------------
    public class UserDto
    {
        // رقم المستخدم
        public int Id { get; set; }

        // اسم الدخول
        public string Username { get; set; } = string.Empty;

        // الرقم المدني
        public string CivilNumber { get; set; } = string.Empty;

        // رقم الهاتف
        public string Number { get; set; } = string.Empty;

        // الدور الحالي: "Admin" أو "User"
        public string Role { get; set; } = string.Empty;

        // السكن المعين للمستخدم (null = لا يوجد)
        public string? HostelName { get; set; }

        // رابط صورة الملف الشخصي
        public string ProfileImageUrl { get; set; } = string.Empty;
    }

    // -------------------------------------------------------------------------
    // طلب تغيير دور المستخدم — يرسله الأدمن
    // -------------------------------------------------------------------------
    public class UpdateUserRoleDto
    {
        // الدور الجديد: "Admin" أو "User"
        public string Role { get; set; } = string.Empty;
    }
}
